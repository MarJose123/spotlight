info:
  name: List of user
  type: http
  seq: 1

http:
  method: GET
  url: "{{domain}}:{{port}}/api/{{version}}/users"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false

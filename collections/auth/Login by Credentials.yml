info:
  name: Login by Credentials
  type: http
  seq: 1

http:
  method: POST
  url: "{{domain}}:{{port}}/api/{{version}}/auth/login"
  body:
    type: json
    data: |-
      {
        "email": "Mitchell27@gmail.com",
        "password": "admin123"
      }

runtime:
  scripts:
    - type: after-response
      code: |-
        if(res.status === 201 && res.body.access_token) {
          const token = res.body.access_token;
          bru.setEnvVar("access_token", token);
          
        }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false

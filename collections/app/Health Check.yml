info:
  name: Health Check
  type: http
  seq: 1

http:
  method: GET
  url: "{{domain}}:{{port}}/api/{{version}}/health"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
